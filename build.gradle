buildscript {
    repositories { google(); mavenCentral(); jcenter() }
    dependencies { classpath 'com.android.tools.build:gradle:7.0.4' }
}
allprojects {
    repositories { google(); mavenCentral(); jcenter() }
}
